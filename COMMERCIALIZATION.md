# UE Agent Bridge 商业化执行清单

> 当前状态：15 工具 / MIT / 全部测试通过
> 目标：Fab Marketplace 付费插件 + 中文社群

---

## 第一阶段：可分发产品（本周 ~ 下周）

这一步的目标是把"能在我机器上跑的项目"变成"别人下载后能跑的产品"。

### 1. 插件去硬编码 ✅

当前问题：`build.bat` 硬编码了 MyProject4 路径，换项目就废。

**具体操作**：
- 将 `build.bat` 改为参数化脚本，用户传项目路径
- 在 README 中列出依赖：UE 5.6 + PythonScriptPlugin + EditorScriptingUtilities

**涉及文件**：`build.bat`

---

### 2. 安装体验：一键 setup ✅

用户最痛的点是安装复杂（C++编译 + npm install + tsc + MCP 配置）。

**具体操作**：
- 重写 `setup.bat`，增加 C++ 编译步骤和 UE 项目路径参数
- 创建 `install.bat`：`install.bat "D:\MyProject\MyProject.uproject"` → 自动完成全部 4 步

**涉及文件**：`setup.bat`（重写）、新建 `install.bat`

---

### 3. MCP 配置自动生成 ✅

当前用户需要手动编辑 `mcp.json`。

**具体操作**：
- 在 `install.bat` 末尾自动生成 `mcp-config.json`，用户直接复制

---

### 4. Demo 关卡 ✅ (脚本就绪)

Fab 审核和用户第一印象都需要看到一个能跑的场景。

**已完成**：
- `demo/setup_demo.py` — UE Python 脚本，通过 `ue_execute_python` 一键生成场景
- `demo/demo-walkthrough.mjs` — MCP 工具演示序列，11/15 个工具
- README 新增 Demo 章节
- 场景内容：8x8 平台 + 立方体塔 + 球体阵列 + 圆柱体 + 锥体 + 文字标签 + BP_DemoActor

**待做**：
- 在 UE Editor 中实际执行 setup_demo.py 生成关卡
- 录一段屏幕录像：AI 对话 → 插件执行 → 物体出现在视口
- 15 秒短视频，不需要讲解

**涉及文件**：`demo/` 目录

---

## 第二阶段：开源引流（第 2~3 周）

让搜索引擎能找到这个项目，让潜在用户能试用。

### 5. 创建独立 GitHub 仓库

当前代码在本地，需要推到公开仓库。

**具体操作**：
- 仓库名：`ue-agent-bridge`
- Description：MIT-licensed MCP bridge for Unreal Engine 5 — let AI agents control the UE Editor
- Topics：`unreal-engine` `mcp` `ai-agent` `ue5` `game-development`
- 添加 LICENSE（MIT）、README（已就绪）、demo 视频链接

---

### 6. B站 demo 视频

一个 3 分钟的视频，展示：
1. 打开 UE 编辑器
2. AI 对话中说"列出关卡中所有 Actor"→ 返回列表
3. "在 (0,0,100) 位置生成一个球"→ 球出现
4. "编译 BP_PopupActor"→ 编译成功
5. 结束：GitHub 链接

---

### 7. UE 中文社区推广

- 在知乎发一篇文章：《我用 MCP 让 AI 操控了 Unreal Engine》
- 在 UE 官方中文论坛发帖
- 标题要点：MIT 开源、15 个工具、比 ue-mcp 快

---

## 第三阶段：Fab 上架（第 3~4 周）

### 8. 决定免费版 vs 付费版的功能线

当前 15 个工具全部 MIT 开源。Fab 上架时有两种策略：

**策略 A — 自用便利版**（推荐新手期）
- Fab 上架的就是当前 15 个工具，标价 $29.99
- 卖点是"预编译 + 一键安装 + 中文支持 + 视频教程"
- 开源版继续在 GitHub 免费，Fab 版免去 C++ 编译痛苦
- 优点：零额外开发，直接上架
- 缺点：懂行的用户会直接用开源版

**策略 B — 功能分层**
- GitHub 免费版：当前 15 个工具（限关卡+蓝图+资产）
- Fab 专业版 $49.99：新增材质编辑 + Niagara 特效参数 + UMG 控件操作
- 需要额外开发 3~5 个工具
- 优点：付费有独占价值
- 缺点：需要额外 2~3 周开发

**建议先走策略 A，快速验证有没有人愿意付钱。**

---

### 9. Fab 商店页面素材

Epic 要求：
- 5 张以上截图（1920x1080）
- 功能列表
- 技术细节（支持的 UE 版本、依赖插件）
- 演示视频链接

**具体操作**：
- 截图 1：MCP 客户端界面，显示 tools/list 返回的 15 个工具
- 截图 2：UE 视口中 AI 命令执行结果
- 截图 3：蓝图读取结果展示
- 截图 4：资产搜索展示
- 截图 5：关卡 Actor 列表
- 写英文商店描述（中英双语）

---

### 10. Fab 打包

**具体操作**：
- 确保 `.uplugin` 中 `VersionName` 更新为 `1.0.0`
- 确保 `CreatedByURL`、`DocsURL`、`SupportURL` 已填写
- 用 Fab 发布工具打包上传
- 等待审核（通常 3-5 个工作日）

---

## 第四阶段：变现（第 4 周起）

### 11. 知识星球

Fab 上架后同步启动中文社群。

- 星球名：UE Agent Bridge 中文社区
- 定价：¥199/年
- 内容：安装指导 + 每周答疑 + 新功能预告 + 企业版优先试用
- 引流：B站视频评论区 + 知乎文章底部 + GitHub README

---

### 12. Fab 上线后维护

- 每周查看 Fab 评论区，回复问题
- 每月发布一次小更新（修 bug、加 1~2 个小工具）
- GitHub Issues 作为 bug 追踪

---

## 时间线总览

```
Week 1        Week 2        Week 3        Week 4+
│             │             │             │
├─去硬编码     ├─GitHub仓库   ├─Fab素材准备  ├─Fab审核中
├─一键安装     ├─B站视频      ├─Fab打包     ├─知识星球启动
├─Demo关卡    ├─论坛推广     └─提交审核     ├─开始收钱
└─录屏        └─             └─            └─维护更新
```

---

## 不做的事

| 不做 | 原因 |
|:--|:--|
| 自建网站 | Fab + GitHub 足够，零维护成本 |
| 英文社区 | 一人精力有限，先吃透中文市场 |
| 功能竞赛（50+工具） | 做不过 ue-mcp，聚焦质量而非数量 |
| 免费+捐赠 | 靠自愿打赏养不活产品 |
| SaaS 订阅制 | UE 插件不适合 SaaS，一次性永久许可更合理 |

---

## 收入模型（纯插件）

| 来源 | 上线 3 个月后月均预估 |
|:--|:--|
| Fab 付费版（$29.99~$49.99） | ¥3,000-8,000 |
| 知识星球（¥199/年） | ¥800-2,000 |
| **月均合计** | **¥3,800-10,000** |
| **年化** | **¥45,000-120,000** |

---

*当前可以立刻开始的是：1. 去硬编码 → 2. 一键安装脚本 → 3. 录 demo 视频 → 4. 推 GitHub*
